# Copyright (c) .NET Foundation and Contributors
# https://github.com/networkfusion/MBN-TinyCLR/ original build, merged with https://github.com/TekuSP/Meteostanice-CSharp/

name: Build Nanoframework

on: [push, pull_request, workflow_dispatch]

defaults:
  run:
    shell: pwsh
jobs:
  Build:
    strategy:
      matrix:
        configuration: [Debug, Release]
    name: Build NanoFramework SLN
    timeout-minutes: 15
    runs-on: windows-latest
    env:
      solution: 'Meteostanice.sln'
      major: '0'
      minor: '4'
    steps:
      - name: Checkout
        uses: actions/checkout@v5.0.0
        with:
          fetch-depth: 0
          
      - name: Setup .NET
        uses: actions/setup-dotnet@v4.3.1
        with:
          dotnet-version: 9.0.x

      - name: Set up JDK 17
        uses: actions/setup-java@v5.0.0
        with:
          java-version: 17
          distribution: 'zulu' # Alternative distribution options are available.

      - name: Setup MSBuild Path
        uses: microsoft/setup-msbuild@v2
        with:
          msbuild-architecture: x64

      # https://github.com/NuGet/setup-nuget
      - name: Install NuGet client
        uses: nuget/setup-nuget@v2.0.1
        with:
          nuget-version: 'latest'
          
      - name: Install Nanoframework Extension
        uses: nanoframework/nanobuild@v1.16
        env:
          GITHUB_AUTH_TOKEN: ${{ secrets.githubAuth }}
          
      - name: Restore nugets
        run: nuget restore ${{ env.solution }} -ConfigFile nuget.config
        
      - name: Cache SonarCloud packages
        uses: actions/cache@v4.2.4
        with:
          path: ~\sonar\cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      - name: Cache SonarCloud scanner
        id: cache-sonar-scanner
        uses: actions/cache@v4.2.4
        with:
          path: .\.sonar\scanner
          key: ${{ runner.os }}-sonar-scanner
          restore-keys: ${{ runner.os }}-sonar-scanner

      - name: Install SonarCloud scanner
        if: steps.cache-sonar-scanner.outputs.cache-hit != 'true'
        shell: powershell
        run: |
          New-Item -Path .\.sonar\scanner -ItemType Directory
          dotnet tool update dotnet-sonarscanner --tool-path .\.sonar\scanner
          
      - name: Begin SonarCloud analysis
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # Needed to get PR information, if any
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        shell: powershell
        run: |
          .\.sonar\scanner\dotnet-sonarscanner begin /k:"TekuSP_Meteostanice-CSharp" /o:"tekusp" /d:sonar.token="${{ secrets.SONAR_TOKEN }}" /d:sonar.scanner.scanAll=false /d:sonar.host.url="https://sonarcloud.io"
          
      - name: Build and test (nanoFramework)
        uses: nanoframework/nanobuild@v1
        with:
          solution: ${{ env.solution }}          # adjust if your .sln path differs
          configuration: ${{ matrix.configuration }}
          runTests: true
          testProjects: |
            **/*Tests.csproj                        # glob(s) for your test projects
          enableCodeCoverage: true

      - name: End SonarCloud analysis
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # Needed to get PR information, if any
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        shell: powershell
        run: |
          .\.sonar\scanner\dotnet-sonarscanner end /d:sonar.token="${{ secrets.SONAR_TOKEN }}"
          
      - name: Prepare nuget version ${{ env.major }}.${{ env.minor }}.${{ github.run_number }}
        uses: richardrigutins/replace-in-files@v2
        with:
          files: '**/*.nuspec'
          search-text: '$version$'
          replacement-text: '${{ env.major }}.${{ env.minor }}.${{ github.run_number }}'
          encoding: 'utf8'
          max-parallelism: 10
      
      - name: Pack nugets
        run: ./GenerateNugets.ps1
        if: ${{ matrix.configuration == 'Release' }}

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4.6.2
        if: ${{ matrix.configuration == 'Release' }}
        with:
          name: TekuSP-Drivers
          path: |
            ${{ github.workspace }}\**\*TekuSP*.nupkg
